import { useCallback, useEffect, useMemo, useState } from "react";
import { BrowserRouter, useLocation, useNavigate } from "react-router-dom";
import axios from "axios";
import { BarChart3, BookOpen, ChevronDown, CircleDollarSign, Download, Filter, LayoutDashboard, LogOut, Menu, Plus, Settings2, Tags, Trash2, Users, WalletCards, X } from "lucide-react";
import { Bar, BarChart, CartesianGrid, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import "@/App.css";

const API=`${process.env.REACT_APP_BACKEND_URL}/api`; const money=v=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(Number(v)||0); const api=(url,opts={})=>axios({url:`${API}${url}`,withCredentials:true,...opts}).then(r=>r.data); const today=()=>new Date().toLocaleDateString("pt-BR");
function AuthCallback(){const nav=useNavigate(), loc=useLocation(); useEffect(()=>{const id=new URLSearchParams(loc.hash.slice(1)).get("session_id"); if(id) api("/auth/session",{method:"POST",data:{session_id:id}}).then(()=>nav("/"));},[loc,nav]); return <main className="auth-page"><div className="loader"/>Validando seu acesso…</main>}
function Login({onLogin}){const [error,setError]=useState(""); const start=()=>{ /* REMINDER: DO NOT HARDCODE THE URL, OR ADD ANY FALLBACKS OR REDIRECT URLS, THIS BREAKS THE AUTH */ window.location.href=`https://auth.emergentagent.com/?redirect=${encodeURIComponent(window.location.origin+"/")}`}; return <main className="auth-page"><div className="auth-art"><div className="brand"><CircleDollarSign/> Meu Controle Financeiro</div><div><span className="eyebrow">Seu dinheiro, mais claro</span><h1>Decisões tranquilas começam com uma visão simples.</h1><p>Organize sua vida financeira, convide quem importa e acompanhe tudo em um só lugar.</p></div></div><section className="auth-box"><div className="brand mobile-brand"><CircleDollarSign/> Meu Controle Financeiro</div><span className="eyebrow">Acesso seguro</span><h2>Bem-vindo de volta</h2><p className="muted">Entre para continuar seu controle financeiro.</p><button data-testid="login-google-button" className="primary wide" onClick={start}>Continuar com Google</button><div className="divider">ou</div><button data-testid="login-demo-button" className="secondary wide" onClick={()=>api("/auth/demo",{method:"POST"}).then(onLogin).catch(()=>setError("Não foi possível iniciar a demonstração."))}>Entrar na demonstração</button>{error&&<div data-testid="login-error" className="error">{error}</div>}<small>Ao continuar, você concorda com nossos termos de uso.</small></section></main>}
function Shell({user,onLogout,children,control,setControl,controls}){const nav=useNavigate(); return <div className="app-shell"><aside><div className="brand"><CircleDollarSign/> Meu Controle</div><div className="control-picker"><span>CONTROLE ATIVO</span><button data-testid="active-control-selector" onClick={()=>controls.length>1&&setControl(controls[(controls.indexOf(control)+1)%controls.length])}>{control?.name||"Selecione um controle"}<ChevronDown size={16}/></button></div><nav><button data-testid="nav-dashboard" className={location.pathname==="/"?"active":""} onClick={()=>nav("/")}><LayoutDashboard/> Visão geral</button><button data-testid="nav-transactions" onClick={()=>nav("/lancamentos")}><BookOpen/> Lançamentos</button><button data-testid="nav-tags" onClick={()=>nav("/tags")}><Tags/> Tags</button><button data-testid="nav-members" onClick={()=>nav("/membros")}><Users/> Pessoas</button></nav><button data-testid="logout-button" className="logout" onClick={onLogout}><LogOut/> Sair</button></aside><main className="content"><header><button className="menu-btn" data-testid="mobile-menu-button"><Menu/></button><div><span className="eyebrow">{new Date().toLocaleDateString("pt-BR",{weekday:"long",day:"numeric",month:"long"})}</span><h1>{control?.name||"Meu controle"}</h1></div><div className="profile"><div className="avatar">{user.name?.[0]}</div><span>{user.name}</span><Settings2 size={17}/></div></header>{children}</main><div className="bottom-nav"><button data-testid="mobile-nav-dashboard" onClick={()=>nav("/")}><LayoutDashboard/>Início</button><button data-testid="mobile-nav-transactions" onClick={()=>nav("/lancamentos")}><BookOpen/>Lançamentos</button><button data-testid="mobile-nav-tags" onClick={()=>nav("/tags")}><Tags/>Tags</button><button data-testid="mobile-nav-members" onClick={()=>nav("/membros")}><Users/>Pessoas</button></div></div>}
function Modal({title,onClose,children}){return <div className="modal-backdrop"><div className="modal"><button data-testid="modal-close-button" className="icon-btn close" onClick={onClose}><X/></button><h2>{title}</h2>{children}</div></div>}
function Filters({filters,setFilters,tags,members}){return <div className="filters"><div className="filter-title"><Filter size={16}/> Filtros</div><select data-testid="filter-period" value={filters.period} onChange={e=>setFilters({...filters,period:e.target.value})}><option value="all">Todo o período</option><option value="month">Este mês</option></select><select data-testid="filter-type" value={filters.type} onChange={e=>setFilters({...filters,type:e.target.value})}><option value="all">Todos os tipos</option><option value="income">Entradas</option><option value="expense">Saídas</option></select><select data-testid="filter-tag" value={filters.tag} onChange={e=>setFilters({...filters,tag:e.target.value})}><option value="all">Todas as tags</option>{tags.map(t=><option key={t.tag_id} value={t.tag_id}>{t.name}</option>)}</select><select data-testid="filter-member" value={filters.user} onChange={e=>setFilters({...filters,user:e.target.value})}><option value="all">Todas as pessoas</option>{members.map(m=><option key={m.user_id} value={m.user_id}>{m.email}</option>)}</select></div>}
function Dashboard({control,tags,members,transactions,onRefresh}){const [filters,setFilters]=useState({period:"all",type:"all",tag:"all",user:"all"}); const [show,setShow]=useState(false); const filtered=useMemo(()=>transactions.filter(t=>(filters.period==="all"||(()=>{const [day,month,year]=t.date.split("/");const current=new Date();return Number(month)===current.getMonth()+1&&Number(year)===current.getFullYear()})())&&(filters.type==="all"||t.type===filters.type)&&(filters.tag==="all"||t.tag_id===filters.tag)&&(filters.user==="all"||t.user_id===filters.user)),[transactions,filters]); const income=filtered.filter(t=>t.type==="income").reduce((a,t)=>a+Number(t.amount),0), expense=filtered.filter(t=>t.type==="expense").reduce((a,t)=>a+Number(t.amount),0); const byTag=tags.map(t=>({name:t.name,value:filtered.filter(x=>x.tag_id===t.tag_id&&x.type==="expense").reduce((a,x)=>a+Number(x.amount),0),color:t.color})).filter(x=>x.value); const trend=[...new Set(filtered.map(t=>t.date))].slice(-6).map(date=>({date,Entradas:filtered.filter(t=>t.date===date&&t.type==="income").reduce((a,t)=>a+Number(t.amount),0),Saídas:filtered.filter(t=>t.date===date&&t.type==="expense").reduce((a,t)=>a+Number(t.amount),0)})); const addDemo=()=>api(`/controls/${control.control_id}/demo-data`,{method:"POST"}).then(onRefresh); const removeDemo=()=>api(`/controls/${control.control_id}/demo-data`,{method:"DELETE"}).then(onRefresh); return <><div className="toolbar"><div><span className="eyebrow">Resumo filtrado</span><p data-testid="dashboard-description" className="muted">Acompanhe o que entrou e saiu do seu controle.</p></div><div className="toolbar-actions"><button data-testid="add-demo-button" className="secondary" onClick={addDemo}><Download size={16}/> Adicionar demonstração</button><button data-testid="remove-demo-button" className="secondary" onClick={removeDemo}><Trash2 size={16}/> Remover demonstração</button><button data-testid="new-transaction-button" className="primary" onClick={()=>setShow(true)}><Plus size={17}/> Novo lançamento</button></div></div><Filters filters={filters} setFilters={setFilters} tags={tags} members={members}/><section className="stats"><div className="stat balance"><span>Saldo atual</span><strong data-testid="balance-value">{money(income-expense)}</strong><small>Entradas menos saídas</small></div><div className="stat"><span>Entradas</span><strong data-testid="income-total" className="positive">{money(income)}</strong><small>No período selecionado</small></div><div className="stat"><span>Saídas</span><strong data-testid="expense-total" className="negative">{money(expense)}</strong><small>No período selecionado</small></div><div className="stat"><span>Lançamentos</span><strong data-testid="transaction-count">{filtered.length}</strong><small>Registros encontrados</small></div></section><section className="charts"><div className="panel chart-large"><div className="panel-head"><div><h3>Fluxo ao longo do tempo</h3><span className="muted">Comparativo diário</span></div><BarChart3 size={19}/></div><ResponsiveContainer width="100%" height={230}><BarChart data={trend}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e2e8f0"/><XAxis dataKey="date"/><YAxis/><Tooltip formatter={money}/><Legend/><Bar dataKey="Entradas" fill="#10b981" radius={[4,4,0,0]}/><Bar dataKey="Saídas" fill="#f97316" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div><div className="panel"><div className="panel-head"><div><h3>Onde você gasta</h3><span className="muted">Por tag</span></div><Tags size={19}/></div>{byTag.length?<ResponsiveContainer width="100%" height={230}><PieChart><Pie data={byTag} dataKey="value" nameKey="name" innerRadius={55} outerRadius={82} paddingAngle={3}>{byTag.map(x=><Cell key={x.name} fill={x.color}/>)}</Pie><Tooltip formatter={money}/><Legend/></PieChart></ResponsiveContainer>:<div data-testid="empty-chart" className="empty">Adicione lançamentos para visualizar</div>}</div></section><section className="panel recent"><div className="panel-head"><div><h3>Últimos lançamentos</h3><span className="muted">Atualizados agora</span></div><button data-testid="view-all-transactions" className="link-btn" onClick={()=>location.href="/lancamentos"}>Ver todos</button></div>{filtered.slice(0,5).map(t=><div className="transaction-row" key={t.transaction_id}><div className={`tx-icon ${t.type}`}><CircleDollarSign size={17}/></div><div className="tx-info"><strong>{t.description}</strong><span>{t.date} · {tags.find(x=>x.tag_id===t.tag_id)?.name||"Sem tag"}</span></div><b className={t.type}>{t.type==="income"?"+":"−"}{money(t.amount)}</b></div>)}{!filtered.length&&<div data-testid="empty-transactions" className="empty">Ainda não há lançamentos neste controle.</div>}</section>{show&&<TransactionModal control={control} tags={tags} onClose={()=>setShow(false)} onSaved={()=>{setShow(false);onRefresh()}}/>}</>}
function TransactionModal({control,tags,onClose,onSaved}){const [form,setForm]=useState({type:"expense",amount:"",date:today(),description:"",tag_id:tags[0]?.tag_id||"",note:""}); const save=e=>{e.preventDefault();api(`/controls/${control.control_id}/transactions`,{method:"POST",data:form}).then(onSaved).catch(()=>alert("Confira os dados do lançamento."))}; return <Modal title="Novo lançamento" onClose={onClose}><form onSubmit={save}><div className="segmented"><button type="button" data-testid="transaction-type-income" className={form.type==="income"?"selected":""} onClick={()=>setForm({...form,type:"income"})}>Entrada</button><button type="button" data-testid="transaction-type-expense" className={form.type==="expense"?"selected":""} onClick={()=>setForm({...form,type:"expense"})}>Saída</button></div><label>Valor<input data-testid="transaction-amount-input" required min="0.01" step="0.01" type="number" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/></label><div className="form-grid"><label>Data<input data-testid="transaction-date-input" required value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><label>Tag<select data-testid="transaction-tag-select" required value={form.tag_id} onChange={e=>setForm({...form,tag_id:e.target.value})}>{tags.map(t=><option key={t.tag_id} value={t.tag_id}>{t.name}</option>)}</select></label></div><label>Descrição<input data-testid="transaction-description-input" required value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label>Observação <input data-testid="transaction-note-input" value={form.note} onChange={e=>setForm({...form,note:e.target.value})}/></label><button data-testid="transaction-save-button" className="primary wide">Salvar lançamento</button></form></Modal>}
function TagsPage({control,tags,onRefresh}){const [form,setForm]=useState({name:"",color:"#078b67",kind:"both"});const save=e=>{e.preventDefault();api(`/controls/${control.control_id}/tags`,{method:"POST",data:form}).then(()=>{setForm({...form,name:""});onRefresh()})};return <section className="panel page-panel"><div className="panel-head"><div><span className="eyebrow">Organização</span><h2>Tags do controle</h2><p className="muted">Use cores para reconhecer rapidamente cada categoria.</p></div><Tags/></div><form className="inline-form" onSubmit={save}><input data-testid="tag-name-input" required placeholder="Nome da tag" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input data-testid="tag-color-input" type="color" value={form.color} onChange={e=>setForm({...form,color:e.target.value})}/><select data-testid="tag-kind-select" value={form.kind} onChange={e=>setForm({...form,kind:e.target.value})}><option value="both">Entradas e saídas</option><option value="income">Entradas</option><option value="expense">Saídas</option></select><button data-testid="tag-save-button" className="primary"><Plus size={16}/> Criar tag</button></form><div className="tag-grid">{tags.map(t=><div className="tag-item" key={t.tag_id}><i style={{background:t.color}}/><strong>{t.name}</strong><span>{t.kind==="both"?"Ambos":t.kind==="income"?"Entrada":"Saída"}</span></div>)}</div></section>}
function MembersPage({control,members}){const [email,setEmail]=useState(""),[role,setRole]=useState("viewer"),[message,setMessage]=useState("");const invite=e=>{e.preventDefault();api(`/controls/${control.control_id}/members`,{method:"POST",data:{email,role}}).then(()=>{setMessage("Pessoa adicionada ao controle.");setEmail("")}).catch(x=>setMessage(x.response?.data?.detail||"Não foi possível adicionar."))};return <section className="panel page-panel"><div className="panel-head"><div><span className="eyebrow">Compartilhamento</span><h2>Pessoas autorizadas</h2><p className="muted">Convide alguém que já acessou o aplicativo.</p></div><Users/></div><form className="inline-form" onSubmit={invite}><input data-testid="member-email-input" type="email" required placeholder="email@exemplo.com" value={email} onChange={e=>setEmail(e.target.value)}/><select data-testid="member-role-select" value={role} onChange={e=>setRole(e.target.value)}><option value="viewer">Visualizador</option><option value="editor">Editor</option></select><button data-testid="member-invite-button" className="primary"><Plus size={16}/> Adicionar</button></form>{message&&<p data-testid="member-feedback" className="muted">{message}</p>}<div className="member-list">{members.map(m=><div className="member-row" key={m.user_id}><div className="avatar">{m.email[0].toUpperCase()}</div><span>{m.email}</span><b>{m.role==="owner"?"Proprietário":m.role==="editor"?"Editor":"Visualizador"}</b></div>)}</div></section>}
function Page({user,onLogout}){const [controls,setControls]=useState([]),[control,setControl]=useState(null),[tags,setTags]=useState([]),[members,setMembers]=useState([]),[transactions,setTransactions]=useState([]); const refresh=useCallback(()=>control&&Promise.all([api(`/controls/${control.control_id}/tags`),api(`/controls/${control.control_id}/members`),api(`/controls/${control.control_id}/transactions`)]).then(([a,b,c])=>{setTags(a);setMembers(b);setTransactions(c)}),[control]); useEffect(()=>{api("/controls").then(x=>{setControls(x);setControl(x[0]||null)});},[]); useEffect(()=>{if(control)refresh()},[control,refresh]); const path=window.location.pathname; if(!control)return <Shell {...{user,onLogout,control,setControl,controls}}><div className="empty-page"><WalletCards size={40}/><h2>Comece seu controle financeiro</h2><p>Crie seu primeiro controle para acompanhar entradas, saídas e objetivos.</p><button data-testid="create-control-button" className="primary" onClick={async()=>{const name=prompt("Nome do controle");if(name){const c=await api("/controls",{method:"POST",data:{name,description:""}});setControls([c]);setControl(c)}}}><Plus/> Criar controle</button></div></Shell>; let view=<Dashboard {...{control,tags,members,transactions,onRefresh:refresh}}/>; if(path==="/tags")view=<TagsPage control={control} tags={tags} onRefresh={refresh}/>; if(path==="/membros")view=<MembersPage control={control} members={members}/>; return <Shell {...{user,onLogout,control,setControl,controls}}>{view}</Shell>}
function AppRouter(){const loc=useLocation(),[user,setUser]=useState(null),[loading,setLoading]=useState(true); useEffect(()=>{if(loc.hash.includes("session_id="))return;api("/auth/me").then(setUser).catch(()=>{}).finally(()=>setLoading(false))},[loc.hash]); if(loc.hash.includes("session_id="))return <AuthCallback/>; if(loading)return <main className="auth-page"><div className="loader"/></main>; if(!user)return <Login onLogin={setUser}/>; return <Page user={user} onLogout={()=>api("/auth/logout",{method:"POST"}).then(()=>setUser(null))}/>}
export default function App(){return <BrowserRouter><AppRouter/></BrowserRouter>}